# 弈金（金融 Agent）项目开发规范

## 1. 项目定位

弈金不是当前阶段的通用金融聊天机器人，也不是自动交易系统。

当前产品定位：

> 面向证券公司和基金公司的投研人员，围绕 A 股上市公司深度研究，提供可上传材料、可联网检索、可追溯核验、可人工复核、可导出报告的金融智能体工作台。

核心价值：减少投研人员在资料搜集、长文档阅读、数据整理、初稿撰写和来源核对上的重复劳动，让专业人员把时间用于判断和复核。

## 2. 当前目标

构建一个接近真实生产系统的 MVP，并通过可复现的评测集证明价值。

首要业务闭环：

```text
用户创建研究任务
  → 上传公司年报/公告等 PDF
  → 联网补充公开信息
  → Commander 生成任务图
  → Specialist 并行或按依赖关系执行
  → 每个 Agent 自检引用、数据和输出质量
  → 汇总报告并自动质量检查
  → 用户人工复核
  → 导出 Markdown/PDF
```

## 3. 已确认需求

### 3.1 用户与权限

- 支持用户注册、登录和退出。
- 开发环境提供“一键进入开发体验”入口，不要求用户先注册。
- 开发体验入口只创建/复用受限的演示用户，生产环境必须关闭。
- 支持多用户和机构空间。
- 用户只能访问自己或所属机构有权限访问的任务、文件和报告。
- 首版基础角色为：管理员、分析师、只读用户。
- 暂不实现 SSO、复杂审批流和商业化计费权限。

### 3.2 研究能力

- 研究对象为 A 股上市公司。
- 支持基本面、行业、消息面、估值和风险分析。
- 报告可以提供研究判断、估值区间、情景分析、风险等级和人工复核事项。
- 不自动执行交易。
- 不生成面向个人用户的个性化买卖指令。
- 报告必须保留风险提示。

### 3.3 文件

- 首版正式支持 PDF。
- 需要支持文本提取、表格提取、文档分块、关键词检索、页码定位和引用回链。
- 文件必须与用户、机构和任务建立关系。
- 必须校验文件类型和大小。
- Excel、Word 和图片/OCR 不属于当前首版范围。

### 3.4 数据与检索

- 首版使用免费或公开数据源。
- 用户上传材料和公开网页检索是当前主要数据来源。
- 结构化财务数据接口暂不纳入当前范围，后续以可插拔方式接入。
- 关键财务数字不得仅凭普通网页搜索或模型记忆生成。
- 找不到可靠来源时必须标记为“待核验”，禁止猜测或补全。
- 每个 Agent 的输出都必须附带引用核验结果。

### 3.5 模型

- DeepSeek 是当前默认模型。
- 后续支持免费在线模型和本地模型。
- 不接入 OpenAI，不以 OpenAI 兼容 API 作为前提。
- 模型调用必须通过统一的 Provider 接口，不能让 Agent 直接依赖某一家模型 SDK。
- 联网检索必须独立于模型 Provider，以便更换模型时保留检索能力。

### 3.6 报告

- 在线展示 Markdown 报告。
- 首版支持 PDF 导出。
- Word 导出暂不纳入当前范围。
- 报告导出前必须经过用户人工复核。
- 未复核内容必须显示“AI 生成草稿”。
- 导出内容必须包含来源、生成时间、风险提示和人工复核状态。

### 3.7 任务执行

- 任务必须由后台 Worker 执行，不能长期占用单个 HTTP 请求。
- 支持任务持久化、SSE 或等效实时进度、断线恢复、失败重试和任务取消。
- 用户刷新页面后可以恢复查看任务状态。
- 任务状态至少包括：pending、running、completed、failed、cancelled。
- Specialist 默认并行执行；存在依赖关系时按 DAG 顺序执行。

### 3.8 部署

- 同时支持云端部署和 Docker 私有化部署。
- 保持一套代码和配置模型。
- 当前不实现 Kubernetes、多区域容灾和复杂运维控制台。

## 4. 明确不做事项

除非用户重新确认，以下内容不进入当前版本：

- 自动交易或交易执行。
- 个性化投资建议。
- 同时覆盖理赔、信贷、财富管理、AML 等多个业务域。
- 12 个以上 Specialist 的无目标扩展。
- Word 导出。
- Excel 导出。
- 扫描 PDF OCR 和复杂图表识别。
- 结构化财务数据接口。
- SSO、复杂审批、计费和企业组织管理。
- Kubernetes 和多区域高可用。

## 5. 总体架构

```text
Web 前端
  ├─ 任务与文件界面
  ├─ 实时进度界面
  ├─ 报告与引用界面
  └─ 人工复核与导出界面
        ↓
API 层
  ├─ 认证与权限
  ├─ 文件与任务 API
  ├─ Agent/Workflow API
  └─ SSE/任务状态订阅
        ↓
任务编排层
  ├─ Commander：理解目标、生成任务图
  ├─ DAG 调度器：并行、依赖、重试、取消
  ├─ Specialist 执行器
  └─ Report Synthesizer
        ↓
质量与证据层
  ├─ 文件解析
  ├─ 检索与引用管理
  ├─ 引用核验
  ├─ 数据/计算核验
  └─ 报告质量审查
        ↓
基础设施
  ├─ PostgreSQL：用户、机构、任务、文件元数据、审计记录
  ├─ Redis：队列、任务状态和短期缓存
  ├─ Worker：长任务执行
  └─ 对象存储抽象：本地开发或 S3 兼容存储
```

## 6. Commander + Specialist 规则

必须保留 Commander + Specialist 架构，但不以 Agent 数量作为产品卖点。

### 6.1 Commander 职责

- 识别研究目标、公司和分析范围。
- 生成结构化任务图，而不是只生成任务列表。
- 为每个任务指定 Agent、输入、输出、优先级和依赖关系。
- 检查 Agent 名称和任务 schema，禁止模型编造不存在的 Agent。
- 处理简单任务、并行任务和有依赖关系的任务。

### 6.2 Specialist 职责

当前核心 Specialist：

- 行业分析
- 基本面分析
- 消息面分析
- 估值/财务建模
- 风险分析
- 报告合成

不为每个新问题盲目创建新 Agent。只有当职责、输入、输出和评测标准明显不同，才增加 Specialist。

### 6.3 统一 Agent 执行协议

每个 Specialist 都必须遵循：

```text
读取任务与证据
  → 生成分析
  → 核验数字、计算和引用
  → 检查完整性、逻辑、格式和风险
  → 输出内容、证据、质量状态和待复核项
```

引用核验和质量审查是所有 Agent 的统一能力，优先通过共享执行协议、工具和 schema 实现，不在每个 Agent 中复制大量相同代码。

### 6.4 DAG 规则

- 没有依赖的任务可以并行。
- 有依赖的任务必须等待上游成功或明确失败。
- 上游结果通过结构化输入传给下游，不能只依赖隐式上下文。
- 失败任务必须记录原因，并根据策略重试或标记失败。
- 禁止无限递归、无限重试和无限 Agent 派生。
- 报告合成必须明确哪些专家成功、失败、超时或被跳过。

## 7. 模型 Provider 规则

推荐接口形态：

```text
LLMProvider
  ├─ complete()
  ├─ stream()
  ├─ structured_output()
  └─ capabilities()
```

Provider 负责：

- 模型调用
- 流式输出
- 结构化输出
- 超时与重试
- 错误分类
- Token 和成本统计
- 能力声明

Search Provider 单独负责：

- 联网搜索
- 公开文件检索
- 搜索结果保存
- URL、标题、摘要和抓取时间
- 引用去重

Agent、DAG 调度器和报告层不得直接调用具体模型 SDK。

## 8. 证据、数据和报告规范

- 原始数据、计算数据和模型判断必须区分。
- 关键数字必须能回到 PDF 页码、表格位置或明确的公开来源。
- 计算结果必须记录公式、输入数据和单位。
- 每个引用必须记录来源地址、标题、定位信息和获取时间（若可得）。
- 证据不足时显示“待核验”，不得使用模型猜测填空。
- 报告必须包含来源和风险提示。
- 研究结论必须说明依据和关键假设。
- 不把搜索摘要直接当作事实数据库。

## 9. 权限与安全红线

- 所有任务、文件和报告查询必须校验用户或机构归属。
- 文件内容不能跨用户或跨机构返回。
- 敏感信息不得写入普通日志。
- API Key、JWT 密钥和数据库密码只能通过环境变量或部署平台 Secret 注入。
- 禁止把真实密钥提交到 Git、文档、前端代码或部署配置。
- 生产环境禁止使用默认 JWT 密钥。
- 模型不能执行交易、发布报告或对外发送金融结论。
- 任何正式报告都必须保留人工复核边界。

仓库现有部署配置曾出现硬编码 API Key。后续生产化前必须完成密钥撤销、替换和敏感信息扫描；在明确执行前，不擅自修改外部平台密钥。

## 10. 评测体系

建立可复现的标准评测集：

- 10–20 家 A 股公司。
- 覆盖消费、制造、医药、科技、金融等行业。
- 每家公司使用固定问题和公开材料。
- 评测 Commander 任务拆解、Agent 选择和依赖关系。
- 评测报告事实、数字、引用、计算、结构、风险提示和可读性。

目标指标：

- 关键数字可追溯率：100%。
- 引用覆盖率：不低于 90%。
- 结构化输出成功率：不低于 95%。
- 任务成功率：不低于 95%。
- 评测必须记录耗时、失败原因、人工修改点和模型配置。

没有真实从业者访谈时，不得声称痛点已经被真实用户验证。应将行业痛点标为研究假设，并用公开材料、标准任务和可用性测试逐步验证。

## 11. 开发顺序

按以下顺序迭代：

1. 修正安全配置和环境管理。
2. 建立文件模型、PDF 解析和对象存储抽象。
3. 建立后台任务模型、Worker、状态机、重试和取消。
4. 将现有编排流程改为结构化 DAG。
5. 抽象 LLM Provider 和 Search Provider。
6. 统一所有 Specialist 的引用核验和质量审查协议。
7. 完成报告质量检查、人工复核和 PDF 导出。
8. 完成用户/机构权限隔离。
9. 建立标准评测集、自动化测试和指标记录。
10. 再考虑结构化财务数据和其他文件格式。

## 12. 测试要求

每项功能必须有可验证的成功标准：

- 文件：可上传、解析、定位引用、拒绝非法文件。
- 任务：可创建、排队、执行、重试、取消和恢复。
- DAG：无依赖任务并行，有依赖任务按顺序执行。
- Agent：输出符合 schema，携带引用和质量状态。
- 权限：用户无法读取其他用户或机构的数据。
- 报告：关键数字可追溯，未复核报告有草稿标记。
- 导出：PDF 内容与在线报告一致。
- 模型：替换 Provider 不需要修改 Agent 编排逻辑。

在添加新功能时，优先编写能重现问题的测试，再实现修复或功能。

## 13. Definition of Done

一个功能只有同时满足以下条件才算完成：

- 需求边界明确，没有偷偷扩展范围。
- 代码与现有架构保持一致。
- 关键路径有测试或可重复验证步骤。
- 错误状态可见且可恢复。
- 权限和敏感数据边界经过检查。
- 文档、schema 和环境配置同步更新。
- 评测集中的相关指标没有退化。

## 14. 工作方式

- 编码前先说明假设、选择和不确定点。
- 能从代码或文档确认的问题，不重复询问用户。
- 存在多个合理方案时，列出取舍并给出推荐方案。
- 遵循手术式修改，只修改完成当前需求所必需的内容。
- 不为了“看起来先进”增加 Agent、抽象或配置项。
- 优先解决真实闭环中的可靠性、证据和权限问题。
- 每次完成后说明改动、验证结果、遗留风险和下一步。
