# 弈金项目协作说明

## 当前状态

2026-09-18，用户已明确删除“公司研究/企业尽调”整套板块。相关前端页面、后端 API、专用模型、专用服务、测试、评测材料和线上数据库专用表已删除或清理。本项目当前只保留通用 Agent、任务、文件附件、证据解析和报告下载能力，等待新的产品方向。

不要恢复或继续扩展以下已删除内容：

- 公司研究页面和 `/api/research-subjects` 路由
- `ResearchSubject`、企业事件、融资需求、行动建议、决策备忘录等专用模型
- 公司研究专用资产生成服务、评测集、回放材料和测试
- 线上数据库中的公司研究专用表或关联列

## 工作方式

- 当前由 Codex 单 agent 执行，不再分配并行 agent。
- 编码前先说明假设、范围和验证标准；能从代码或文档确认的事项不重复询问。
- 只做用户要求的手术式修改，不顺手重构相邻代码。
- 复杂任务先梳理事实、风险和停止条件；不确定时明确指出，不掩饰猜测。
- 每次改动后都要运行与改动直接相关的检查，并区分已实现、已验证和待验证。
- 不把 API Key、JWT 密钥或数据库密码写入 Git、文档、前端代码或部署配置。

## 保留范围

- Web 登录、演示用户和基础权限
- Agent 列表、单 Agent 执行和通用编排任务
- 任务历史、SSE 进度和搜索引用
- PDF、DOCX、Markdown 文件上传、文本提取和页码/片段证据
- 通用 Markdown/DOCX/PDF 报告生成与下载
- 本地开发和云端部署配置

## 验证标准

删除后必须确认：

1. 后端可以导入和启动，且不再注册公司研究路由。
2. 保留的 Agent、任务、文件和报告接口仍能构建。
3. 前端生产构建通过，侧栏不再出现公司研究入口。
4. 仓库中不存在已删除模块的 API、模型、页面、专用服务、测试和评测材料。
5. 线上数据库不再包含公司研究专用表或通用表中的公司研究关联列；通用用户、任务和文件数据不被误删。
6. 部署后 `/health`、登录、任务列表和文件接口完成线上回归。

## 部署规则

- 只有本地构建和检查通过后才推送和部署。
- 线上数据库的删除必须使用明确表名/列名，并先盘点后执行。
- 临时开放数据库管理访问后必须立即恢复原安全配置。
- 不重复部署中间版本；一次完成推送、后端部署、前端部署和回归。

## 当前唯一下一步

本轮删除、部署和线上回归已完成。等待用户重新确认产品方向，不自行恢复公司研究模块。
