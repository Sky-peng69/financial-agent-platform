# 弈金项目协作说明

## 当前产品方向

弈金当前核心是面向银行公司金融与风险管理场景的企业金融动态尽调与决策工作台。

核心闭环：企业材料 → 证据 → 企业事实/风险判断 → 金融行动建议 → 经营事件影响分析 → 待人工复核动作 → 审计记录 → 尽调备忘录或业务工作包。

产品中心是企业尽调，不是普通公司研究报告生成器。报告是导出物，不是唯一结果。

## 已实现的企业尽调能力

- 企业尽调档案创建和权限隔离
- PDF 材料上传、解析、页码级证据
- 基于材料生成判断、假设、反方挑战和决策备忘录
- 融资需求和金融行动建议生成
- 企业事件记录与事件影响分析
- 行动建议、融资需求的确认/驳回和审计历史
- 通用 Agent、任务、SSE 进度和报告导出

## 工作方式

- 当前由 Codex 单 agent 执行，不再并行分配同一条任务。
- 编码前明确假设、范围和成功标准；只做手术式修改。
- 先修复会阻断真实闭环的问题，再扩展功能。
- 每项功能必须有测试或可重复的验收步骤。
- 不把 API Key、JWT 密钥或数据库密码写入 Git、文档、前端代码或部署配置。
- 不自动授信、调额、支付、交易或对外发送金融结论；正式动作必须人工复核。

## 验证标准

- 前端入口和页面能展示企业尽调主链路，而不是只展示普通任务。
- 后端 API、模型、数据库和前端 schema 保持一致。
- 证据、数字、风险判断和金融动作可追溯。
- 跨用户访问返回 404/403，不泄露企业材料。
- 关键链路有后端测试；当前恢复后的后端测试目标为 21 项。
- 部署后必须完成登录、创建企业档案、上传材料、生成资产、记录事件和人工复核回归。

## 当前下一步

恢复后的企业尽调功能已经通过本地前端构建、21 项后端测试和线上路由/API 验收；不要删除企业尽调模块。
