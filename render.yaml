services:
  # FastAPI 后端
  - type: web
    name: yijin-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: yijin-db
          property: connectionString
      - key: REDIS_URL
        sync: false
      - key: FRONTEND_URL
        value: https://yijin-301.netlify.app
    plan: free

databases:
  - name: yijin-db
    plan: free
