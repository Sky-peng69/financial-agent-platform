% ============================================================
% 弈金 · 工行杯策划书 — Pandoc LaTeX 模板
% 风格：投行研报风（深 Navy + 紫色强调 + PingFang SC）
% ============================================================
\PassOptionsToPackage{table,svgnames}{xcolor}
\documentclass[a4paper,12pt,fontset=none]{ctexart}

% ==================== 包加载（必须在所有配置之前）====================
\usepackage[a4paper, left=2.5cm, right=2.5cm, top=2.8cm, bottom=2.5cm, headheight=32pt]{geometry}
\usepackage{tikz}
\usetikzlibrary{calc, positioning, shapes, backgrounds, decorations.pathreplacing}
\usepackage{fancyhdr}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{colortbl}
\usepackage{xcolor}
\usepackage{caption}
\usepackage{setspace}
\usepackage{enumitem}
\usepackage{listings}
\usepackage{hyperref}
\usepackage{framed}
\usepackage{needspace}
\usepackage{graphicx}
\usepackage{array}
\usepackage{calc}

% Pandoc 生成的表格使用 \real{...} 来缩放列宽，但该命令在某些 LaTeX
% 发行版中不可用。这里定义为恒等映射，依赖现代 \dimexpr 的 * <decimal> 支持。
\newcommand{\real}[1]{#1}

% Pandoc 3.x 用 \pandocbounded 包裹代码块，需要在自定义模板中定义
\newcommand{\pandocbounded}[1]{#1}

\usepackage{ragged2e}
\usepackage{parskip}
\usepackage{letltxmacro}
\usepackage{longtable}
\usepackage{etoolbox}

% ==================== 字体设置 ====================
\setCJKmainfont{Hiragino Sans GB}
\setmainfont{Hiragino Sans GB}
\setmonofont{Menlo}[Scale=0.88]

% ==================== 颜色定义 ====================
\definecolor{deepnavy}{HTML}{1A2744}
\colorlet{DEEPNAVY}{deepnavy}
\definecolor{icbcred}{HTML}{C8102E}
\definecolor{icbcdarkred}{HTML}{8F1028}
\definecolor{icbcgold}{HTML}{B58A45}
\definecolor{warmwhite}{HTML}{FBFAF7}
\definecolor{purple}{HTML}{6C5CE7}
\colorlet{PURPLE}{purple}
\definecolor{lightbluebg}{HTML}{EBF0F7}
\definecolor{lightbluealt}{HTML}{F5F7FB}
\definecolor{darkslate}{HTML}{2D3436}
\definecolor{codetext}{HTML}{DFE6E9}
\definecolor{tableheadbg}{HTML}{1A2744}
\definecolor{tablerowalt}{HTML}{F0F3F8}
\definecolor{quoteborder}{HTML}{6C5CE7}

% ==================== 超链接 ====================
\hypersetup{
  colorlinks=true,
  linkcolor=purple,
  urlcolor=purple,
  citecolor=purple,
  pdfborder={0 0 0}
}

% ==================== 行距与段落 ====================
\onehalfspacing
\setlength{\parskip}{0.4em}
\setlength{\parindent}{0pt}

% ==================== 页眉页脚 ====================
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\footnotesize\color{deepnavy}弈金 · 工行杯策划书}
\fancyhead[R]{\footnotesize\color{deepnavy}\leftmark}
\fancyfoot[C]{\footnotesize\color{deepnavy}—\ \thepage\ —}
\renewcommand{\headrulewidth}{0.5pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{deepnavy!40}\leaders\hrule height \headrulewidth\hfill}}

% 正文页的低对比度研究数据背景：只做氛围，不干扰阅读
\AddToHook{shipout/background}{%
  \begin{tikzpicture}[remember picture, overlay]
    \draw[deepnavy!5, line width=0.35pt] ($$(current page.north east)+(-2.15cm,-1.0cm)$$) -- ($$(current page.south east)+(-2.15cm,1.0cm)$$);
    \draw[deepnavy!5, line width=0.35pt] ($$(current page.north east)+(-3.0cm,-1.0cm)$$) -- ($$(current page.south east)+(-3.0cm,1.0cm)$$);
    \draw[deepnavy!5, line width=0.35pt] ($$(current page.north east)+(-4.0cm,-5.3cm)$$) -- ($$(current page.north east)+(-1.2cm,-5.3cm)$$);
    \fill[icbcred!18] ($$(current page.north east)+(-2.15cm,-5.3cm)$$) circle (0.07cm);
    \fill[icbcgold!28] ($$(current page.north east)+(-3.0cm,-8.2cm)$$) circle (0.05cm);
    % 研究档案卡片：来源、模型、人工复核三层留痕
    \draw[deepnavy!7, line width=0.45pt] ($$(current page.north east)+(-3.55cm,-5.0cm)$$) rectangle ($$(current page.north east)+(-1.35cm,-3.65cm)$$);
    \draw[icbcgold!14, line width=0.45pt] ($$(current page.north east)+(-3.35cm,-7.0cm)$$) rectangle ($$(current page.north east)+(-1.25cm,-5.7cm)$$);
    \draw[icbcred!13, line width=0.45pt] ($$(current page.north east)+(-3.15cm,-9.0cm)$$) rectangle ($$(current page.north east)+(-1.45cm,-7.75cm)$$);
  \end{tikzpicture}%
}

% ---- 首页样式（标题页之后重置）----
\fancypagestyle{plain}{%
  \fancyhf{}
  \fancyfoot[C]{\footnotesize\color{deepnavy}—\ \thepage\ —}
}

% ==================== 关闭自动编号（正文已含中文编号）====================
\setcounter{secnumdepth}{0}

% Pandoc 使用 \LTcaptype{none} 来禁止表格编号，需要定义 none 计数器
\newcounter{none}

% ==================== 章节标题样式 ====================
\ctexset{
  section = {
    format = \LARGE\bfseries\color{deepnavy},
    aftername = \hspace{0.5em},
    beforeskip = 1.5em,
    afterskip = 0.8em
  },
  subsection = {
    format = \large\bfseries\color{deepnavy!90},
    aftername = \hspace{0.4em},
    beforeskip = 1.2em,
    afterskip = 0.5em
  },
  subsubsection = {
    format = \normalsize\bfseries\color{deepnavy!80},
    aftername = \hspace{0.3em},
    beforeskip = 0.8em,
    afterskip = 0.3em
  }
}


% ==================== 重点判断样式（红金证据条）====================
\renewenvironment{quote}{%
  \par\vspace{0.45em}%
  \leftskip=0.6em\rightskip=0.6em
  \noindent\color{icbcred}\rule{3pt}{1.15em}\hspace{0.55em}%
  \small\color{deepnavy!85}\itshape\ignorespaces
}{%
  \par\vspace{0.45em}%
}

% ==================== 代码块样式（暗 slate 背景 + 等宽字体）====================
\lstset{
  basicstyle=\small\ttfamily\color{codetext},
  backgroundcolor=\color{darkslate},
  frame=none,
  framerule=0pt,
  rulecolor=\color{darkslate},
  aboveskip=0.8em,
  belowskip=0.8em,
  columns=flexible,
  keepspaces=true,
  showstringspaces=false,
  breaklines=true,
  breakatwhitespace=false,
  postbreak={\mbox{\textcolor{purple}{{$$\hookrightarrow$$}}}\space},
  xleftmargin=0pt,
  xrightmargin=0pt,
  framesep=8pt,
  framexleftmargin=8pt,
  framexrightmargin=8pt,
  escapeinside={}
}

% ==================== 表格与图片标题样式 ====================
\captionsetup[table]{
  font={small,color=deepnavy},
  labelfont={bf,color=deepnavy},
  skip=6pt
}

\captionsetup[figure]{
  font={small,color=deepnavy!80},
  labelfont={bf,color=deepnavy},
  skip=6pt
}

% ==================== 列表样式 ====================
\setlist[itemize]{nosep, leftmargin=*, itemsep=0.1em}
\setlist[enumerate]{nosep, leftmargin=*, itemsep=0.1em}

% ==================== 封面 TikZ 图案（左侧 Agent 网络节点）====================
\newcommand{\covergeometry}{%
  \begin{tikzpicture}[scale=0.15]
    % 背景节点网络
    \foreach \x in {0,8,...,80} {
      \foreach \y in {0,12,...,120} {
        \pgfmathsetmacro{\s}{random(0,100)}
        \ifnum\s<18
          \fill[white!10] (\x,\y) circle (0.6);
        \fi
        \ifnum\s<6
          \fill[white!20] (\x,\y) circle (1.0);
        \fi
      }
    }
    % 连接线
    \foreach \i in {1,...,25} {
      \pgfmathsetmacro{\xa}{random(0,80)}
      \pgfmathsetmacro{\ya}{random(0,120)}
      \pgfmathsetmacro{\xb}{\xa + random(-20,20)}
      \pgfmathsetmacro{\yb}{\ya + random(-25,25)}
      \draw[white!8, line width=0.3] (\xa,\ya) -- (\xb,\yb);
    }
    % 亮点节点
    \foreach \i in {1,...,8} {
      \pgfmathsetmacro{\x}{random(10,70)}
      \pgfmathsetmacro{\y}{random(15,105)}
      \fill[white!30] (\x,\y) circle (2.0);
      \draw[white!15, line width=0.5] (\x,\y) circle (3.5);
    }
  \end{tikzpicture}%
}

% ==================== 文档开始 ====================
\begin{document}

% Pandoc 用于紧凑列表的辅助命令
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}%
  \setlength{\parskip}{0pt}%
}

% ==================== 封面 ====================
\begin{titlepage}
  \thispagestyle{empty}
  \pagecolor{warmwhite}
  \color{deepnavy}

  % 工行红竖向识别带与金色边界线
  \begin{tikzpicture}[remember picture, overlay]
    \fill[icbcred] (current page.north west) rectangle ($$(current page.south west)+(1.35cm,0)$$);
    \fill[icbcdarkred] ($$(current page.north west)+(1.35cm,0)$$) rectangle ($$(current page.south west)+(1.48cm,0)$$);
    \draw[icbcgold, line width=1.2pt] ($$(current page.north west)+(2.25cm,-2.05cm)$$) -- ($$(current page.north east)+(-2.25cm,-2.05cm)$$);
    \draw[icbcgold, line width=1.2pt] ($$(current page.south west)+(2.25cm,2.05cm)$$) -- ($$(current page.south east)+(-2.25cm,2.05cm)$$);
    % 研究数据网格：表达“数据—证据—决策”的连接
    \foreach \x in {11,13,15,17} {
      \draw[deepnavy!10, line width=0.4pt] (\x cm,3.0cm) -- (\x cm,18.0cm);
    }
    \foreach \y in {5,7,9,11,13,15,17} {
      \draw[deepnavy!10, line width=0.4pt] (3.0cm,\y cm) -- (18.0cm,\y cm);
    }
    \fill[icbcred] (16.8cm,17.2cm) circle (0.16cm);
    \fill[icbcgold] (14.4cm,13.0cm) circle (0.12cm);
    \fill[deepnavy] (17.6cm,9.0cm) circle (0.10cm);
    \draw[icbcred, line width=1.1pt] (14.4cm,13.0cm) -- (16.8cm,17.2cm) -- (17.6cm,9.0cm);
  \end{tikzpicture}

  \vspace*{2.0cm}
  \hspace*{1.0cm}
  \begin{minipage}{0.86\textwidth}
    \raggedright
    {\small\color{icbcred}\bfseries 第十七届“工行杯”全国大学生金融科技创新大赛}

    \vspace{0.55cm}
    {\fontsize{48}{56}\selectfont\bfseries 弈金}

    \vspace{0.12cm}
    {\large\color{icbcred}\bfseries DIGITAL FINANCE / RESEARCH WORKBENCH}

    \vspace{1.0cm}
    {\fontsize{25}{35}\selectfont\bfseries 面向银行投研场景的\par}
    \vspace{0.15cm}
    {\fontsize{25}{35}\selectfont\bfseries 可追溯上市公司智能研究工作台}

    \vspace{0.85cm}
    \begin{tikzpicture}
      \fill[icbcred] (0,0) rectangle (1.4cm,0.12cm);
      \fill[icbcgold] (1.55cm,0) rectangle (5.2cm,0.12cm);
    \end{tikzpicture}

    \vspace{0.8cm}
    {\large\color{deepnavy!75} 数字金融项目策划书}

    \vfill

    \begin{minipage}{0.72\textwidth}
      \small\color{deepnavy!75}
      \textbf{核心命题}\quad 把资料搜集、专业分析、证据核验与人工复核\par
      组织成一条可追溯、可复用、可审查的金融研究工作流。
    \end{minipage}

    \vspace{1.0cm}
    {\small\color{icbcred}\bfseries 2026 年 9 月}
  \end{minipage}

  \begin{tikzpicture}[remember picture, overlay]
    % IP 形象：弈研，作为封面右下角的研究伙伴
    \node[anchor=south east] at ($$(current page.south east)+(-2.0cm,4.0cm)$$) {\includegraphics[width=5.0cm]{assets/yijin-mascot.png}};
    \node[anchor=south west, font=\footnotesize\color{deepnavy!60}] at ($$(current page.south west)+(2.25cm,2.45cm)$$) {让金融研究更快、更稳、更可核验};
    \node[anchor=south east, font=\footnotesize\color{deepnavy!45}] at ($$(current page.south east)+(-2.25cm,2.45cm)$$) {YIJIN / 2026};
  \end{tikzpicture}
\end{titlepage}

% 恢复白色背景
\pagecolor{white}
\color{black}

% ==================== 目录 ====================
\renewcommand{\contentsname}{\centering\LARGE\color{deepnavy}\bfseries 目\quad 录}
\tableofcontents
\newpage

% ==================== 正文开始 ====================

$body$

% ==================== 文档结束 ====================
\end{document}
