# 下一步

## 唯一动作

完成删除后的本地构建检查，推送并部署前后端，然后做线上回归。

## 验证清单

- 后端导入/启动成功，健康检查返回 200。
- 前端生产构建成功，侧栏没有公司研究入口。
- `/api/research-subjects` 不再存在。
- 登录、任务列表、Agent 列表仍可用。
- 线上数据库仍只保留通用用户、任务、文件和报告结构。

## 停止条件

以上检查通过后停止，不继续扩展业务，等待用户确认新的产品方向。
