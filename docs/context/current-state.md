# 当前状态

## 已完成

- 用户已决定删除公司研究/企业尽调板块。
- 已删除公司研究前端页面、侧栏入口、后端路由、专用模型、schema 和专用服务。
- 已删除公司研究专用测试、评测集、回放材料、设计计划和协同材料。
- 已从通用文件、证据、报告模型和 API 中移除公司研究关联字段。
- 线上数据库已清理公司研究专用表及历史数据；盘点时相关表均为 0 行；通用任务、用户和文件表保留。
- 线上数据库临时 allow list 已恢复为空。

## 当前保留能力

通用登录、演示用户、Agent、任务、SSE 进度、文件上传/解析、证据查询、报告生成和报告下载。

## 当前验证状态

- Python 语法检查：已通过。
- 后端完整运行依赖：已通过部署启动和 `/health` 回归。
- 前端生产构建：已通过。
- 删除后线上回归：已通过；旧 API 返回 404，旧页面返回 404，工作台返回 200。

## 当前风险

产品新方向尚未确认；本轮只完成删除、部署和保留通用工作台，不新增业务模块。

## 协作状态

后续由 Codex 单 agent 执行，不再并行分配任务。
